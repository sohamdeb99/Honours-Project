[build]
  base = "/"
  publish = "dist/"
  command = "npm run build"

[[redirects]]
from = "/*"
to = "/index.html"  # Ensure this points to the correct `index.html` in your `dist/` directory
status = 200
force = true  # This ensures that the redirect overrides any existing files
